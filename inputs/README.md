# Inputs

This smoke-test branch intentionally uses no extra uploaded files beyond the root `instructions.md`.

If you want to run it through bobinabox:

- choose `instructions.md` as the instructions file
- leave additional uploads empty
